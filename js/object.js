const tour = {
    place: 'saint-martin',
    cost: 3000,
    duration: '3 days',
    hotel: 'rodela bikel',
    isBeautiful: true
}
const { place: where, cost: money } = tour;
// console.log(where, money)
const { place, cost } = tour;
// console.log(place, cost)
const numbers = [7, 14, 21, 28, 77, 84, 91, 98];
const [, balance] = numbers
console.log(balance)